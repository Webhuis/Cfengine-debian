
body common control

{
bundlesequence  => { "example" };
}

###########################################################

bundle agent example

{     
vars:

  "no" int => countlinesmatching("mark:.*","/etc/passwd");

reports:

  cfengine_3::

    "Found $(no) lines matching";

}

