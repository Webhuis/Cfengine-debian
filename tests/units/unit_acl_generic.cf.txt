***********************************************************
 Promises 
***********************************************************
Bundle acls in the context of agent

   ARGS:

   {
   TYPE: files

      files promise by '/media/flash/acl/test_dir' (implicit) if context is any

                acl => test body for type acl(no parameters)
               {
               acl_method => overwrite if sub-body context any
               acl_type => posix if sub-body context any
               aces =>  {'user:*:rwx','group:*:rx','all:r'} if sub-body context any
               }
       if body context any
            Promise (version not specified) belongs to bundle 'acls' (type agent) in file '../tests/units/unit_acl_generic.cf' near line 16

   }




All Bodies

control body for type common(no parameters)
   {
   bundlesequence =>  {'acls'} if sub-body context any
   }

test body for type acl(no parameters)
   {
   acl_method => overwrite if sub-body context any
   acl_type => posix if sub-body context any
   aces =>  {'user:*:rwx','group:*:rx','all:r'} if sub-body context any
   }

emptyall body for type acl(no parameters)
   {
   aces =>  {'user:*:rwx','group:*:rx','all:'} if sub-body context any
   }

