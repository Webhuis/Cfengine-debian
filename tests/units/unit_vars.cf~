body common control
{
bundlesequence => {"yes"};
}


bundle agent yes
{
vars:

  "path1" string => "/one/two/last1",
         comment >= "This is a comment";
 
reports:

  Yr2009::

    "Last = $(last1),$(last2),$(last3)";

}