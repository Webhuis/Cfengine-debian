to fix the name of /usr/share/<packagename> to cfengine3 rather than
cfengine
--- a/configure.ac
+++ b/configure.ac
@@ -14,7 +14,7 @@
 dnl The version in the next line is the only one to set
 dnl
 
-AM_INIT_AUTOMAKE(cfengine, 3.0.2b1)   dnl remember to set version
+AM_INIT_AUTOMAKE(cfengine3, 3.0.2b1)   dnl remember to set version
 
 AM_CONFIG_HEADER(src/conf.h)
 
