to fix a small typo in cf-agent

--- a/src/agent.c
+++ b/src/agent.c
@@ -1008,7 +1008,7 @@
 {
 if (putenv(s) != 0)
    {
-   CfOut(cf_inform,"putenv","Failed to set environement %s",s);
+   CfOut(cf_inform,"putenv","Failed to set environment %s",s);
    }
 }
 
